// Superseded by FilterBar.tsx — filters were moved from a left sidebar to a top bar per the
// confirmed UX direction. Kept as a re-export so nothing that still imports the old path breaks.
export { FilterBar as FilterSidebar } from "./FilterBar";
