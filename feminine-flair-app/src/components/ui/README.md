Populate this folder with shadcn/ui primitives as needed:
  npx shadcn-ui@latest add button dialog drawer dropdown-menu accordion select

Used across the app: Button, Dialog (product quick view), Drawer (cart), Accordion (product details/care), Select (sort dropdown).
